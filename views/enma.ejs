<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title> Enma - Thrill Agency</title>

<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css"/>
<link rel="icon" type="image/x-icon" href="assets/imgs/logo/favicon.jpg">

  <!-- Google Fonts -->
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Kanit:wght@300;400;500;600;700&amp;display=swap" rel="stylesheet">

  <!-- All CSS files -->
  <link rel="stylesheet" href="assets/css/bootstrap.min.css">
  <link rel="stylesheet" href="assets/css/all.min.css">
  <link rel="stylesheet" href="assets/css/swiper-bundle.min.css">
  <link rel="stylesheet" href="assets/css/progressbar.css">
  <link rel="stylesheet" href="assets/css/meanmenu.min.css">
  <link rel="stylesheet" href="assets/css/master.css">
  <link rel="stylesheet" href="style.css">

<style>
body {
  margin: 0;
  background: #0a0a0a;
  font-family: "Kanit", sans-serif;
  color: #fff;
}

/* ================= TITLE ================= */
.page-header {
  padding: 140px 0 80px;
  text-align: center;
}

.page-header h1 {
  font-size: clamp(2.5rem, 4vw, 4rem);
  font-weight: 600;
  margin: 0;
}

.page-header p {
  margin-top: 14px;
  font-size: 16px;
  color: #9f9f9f;
}

/* ================= SECTION ================= */
.reels-portfolio {
  padding-bottom: 120px;
}

.container {
  max-width: 1400px;
  margin: auto;
  padding: 0 24px;
}

/* ================= GRID ================= */
.reels-grid {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 28px;
}

/* ================= CARD ================= */
.reel-card {
  position: relative;
  border-radius: 22px;
  overflow: hidden;
  background: #000;
  cursor: pointer;
}

/* 9:16 */
.reel-card .media {
  aspect-ratio: 4 / 5 ;
  overflow: hidden;
}

/* 9:16 */
.reel-card .media2 {
  aspect-ratio: 9 / 16 ;
  overflow: hidden;
}

.reel-card img,
.reel-card video {
  width: 100%;
  height: 100%;
  object-fit: cover;
  transition: transform .5s ease, filter .4s ease;
}

/* Overlay */
.reel-card .overlay {
  position: absolute;
  inset: 0;
  padding: 22px;
  display: flex;
  flex-direction: column;
  justify-content: flex-end;
  gap: 8px;
  opacity: 0;
  visibility: hidden;
  transition: opacity .35s ease;
}

.reel-card.video:hover .play-icon {
  opacity: 0;
}

.play-icon {
  position: absolute;
  inset: 0;
  z-index: 3;                 /* 👈 أعلى من الفيديو */
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 44px;
  color: #fff;
  opacity: 1;                 /* 👈 ظاهرة */
  transition: opacity .25s ease;
  pointer-events: none;
}

/* ✅ CSS HOVER ONLY (NO JS) */
.reel-card:hover .overlay {
  opacity: 1;
  visibility: visible;
}

.reel-card:hover img,
.reel-card:hover video {
  filter: brightness(0.55);
  transform: scale(1.05);

}

/* Text */
.overlay h4 {
  margin: 0;
  font-size: 16px;
}

.overlay p {
  margin: 0;
  font-size: 13px;
  color: #ccc;
}

/* Tag */
.tag {
  font-size: 11px;
  padding: 4px 12px;
  border-radius: 100px;
  background: #ff0050;
  width: fit-content;
}

/* Play Icon */
.play-icon {
  position: absolute;
  inset: 0;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 42px;
  color: #fff;
  opacity: 0;
  transition: opacity .3s ease;
  pointer-events: none;
}

/* PLAY ICON – visible by default */
.play-icon {
  position: absolute;
  inset: 0;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 42px;
  color: #fff;
  opacity: 1;              /* 👈 ظاهرة */
  transition: opacity .3s ease;
  pointer-events: none;
}

/* ❌ تختفي عند hover */
.reel-card.video:hover .play-icon {
  opacity: 0;
}

.overlay h4 {
  margin: 4px 0 0;
  font-size: 16px;
  font-weight: 500;
}

.overlay p {
  margin: 0;
  font-size: 13px;
  color: #d0d0d0;
}

/* TAG */
.tag {
  width: fit-content;
  font-size: 11px;
  padding: 4px 12px;
  border-radius: 100px;
  background: #ff0050;
}

/* PLAY ICON */
.play-icon {
  position: absolute;
  inset: 0;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 44px;
  color: #fff;
  opacity: 1;
  transition: opacity .3s ease;
  pointer-events: none;
}



/* ================= CTA ================= */
.cta {
  border-top: 1px solid rgba(255,255,255,.08);
  padding: 100px 24px;
  text-align: center;
}

.cta span {
  display: block;
  font-size: 13px;
  letter-spacing: 0.12em;
  color: #888;
  margin-bottom: 14px;
}

.cta h2 {
  font-size: clamp(2rem, 3vw, 2.8rem);
  font-weight: 600;
  margin: 0;
}

.cta a {
  display: inline-flex;
  align-items: center;
  gap: 10px;
  margin-top: 32px;
  padding: 14px 28px;
  border-radius: 100px;
  background: #fff;
  color: #000;
  font-size: 15px;
  text-decoration: none;
  transition: transform .3s ease;
}

.cta a:hover {
  transform: translateY(-3px);
}

.media-popup {
  position: fixed;
  inset: 0;
  z-index: 99999;
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 28px;
  background: rgba(0, 0, 0, .88);
  opacity: 0;
  visibility: hidden;
  transition: opacity .25s ease, visibility .25s ease;
}

.media-popup.is-open {
  opacity: 1;
  visibility: visible;
}

.media-popup__content {
  position: relative;
  width: min(92vw, 720px);
  max-height: 90vh;
  display: flex;
  align-items: center;
  justify-content: center;
}

.media-popup__stage {
  width: 100%;
  display: flex;
  align-items: center;
  justify-content: center;
}

.media-popup__stage img,
.media-popup__stage video {
  max-width: 100%;
  max-height: 90vh;
  border-radius: 18px;
  object-fit: contain;
  background: #000;
}

.media-popup__stage video {
  width: min(92vw, 520px);
}

.media-popup__close,
.media-popup__nav {
  position: absolute;
  z-index: 2;
  border: 0;
  color: #fff;
  background: rgba(0, 0, 0, .62);
  cursor: pointer;
  transition: background .2s ease, transform .2s ease;
}

.media-popup__close:hover,
.media-popup__nav:hover {
  background: rgba(255, 255, 255, .16);
}

.media-popup__close {
  top: -18px;
  right: -18px;
  width: 44px;
  height: 44px;
  border-radius: 50%;
  font-size: 20px;
}

.media-popup__nav {
  top: 50%;
  width: 44px;
  height: 58px;
  border-radius: 14px;
  font-size: 22px;
  transform: translateY(-50%);
}

.media-popup__nav:hover {
  transform: translateY(-50%) scale(1.04);
}

.media-popup__prev {
  left: -58px;
}

.media-popup__next {
  right: -58px;
}

.media-popup__counter {
  position: absolute;
  left: 50%;
  bottom: -34px;
  transform: translateX(-50%);
  min-width: 54px;
  padding: 5px 12px;
  border-radius: 99px;
  background: rgba(0, 0, 0, .62);
  color: #fff;
  font-size: 13px;
  text-align: center;
}

.media-popup__nav[hidden],
.media-popup__counter[hidden] {
  display: none;
}

body.media-popup-open {
  overflow: hidden;
}

/* ================= RESPONSIVE ================= */
@media (max-width: 1200px) {
  .reels-grid { grid-template-columns: repeat(3, 1fr); }
}

@media (max-width: 991px) {
  .reels-grid { grid-template-columns: repeat(2, 1fr); }
}

@media (max-width: 575px) {
  .reels-grid { grid-template-columns: 1fr; }
  .media-popup {
    padding: 18px;
  }
  .media-popup__content {
    max-height: 86vh;
  }
  .media-popup__stage img,
  .media-popup__stage video {
    max-height: 86vh;
  }
  .media-popup__close {
    top: 10px;
    right: 10px;
  }
  .media-popup__prev {
    left: 10px;
  }
  .media-popup__next {
    right: 10px;
  }
  .media-popup__nav {
    background: rgba(0, 0, 0, .72);
  }
  .media-popup__counter {
    bottom: 12px;
  }
}
</style>
</head>

<body class="dark">

  <!-- Cursor Animation -->
  <div class="cursor1"></div>
  <div class="cursor2"></div>

  <%- include('components/loading.ejs'); -%>

  <!-- Scroll Smoother -->
  <div class="has-smooth" id="has_smooth"></div>

  <!-- Go Top Button -->
  <button id="scroll_top" class="scroll-top"><i class="fa-solid fa-arrow-up"></i></button>

  <!-- Header area start -->
  <%- include('components/header'); -%>
  <!-- Header area end -->
 <div id="smooth-wrapper">
    <div id="smooth-content">
      <main>
<!-- ===== TITLE ===== -->
<section class="page-header">
  <div class="container">
    <h1> ENMA BAY  </h1>
    <p>Social Media Content</p>
  </div>
</section>

<!-- ===== REELS ===== -->
<section class="reels-portfolio">
  <div class="container">
    <div class="reels-grid">


       <div class="reel-card video">
        <div class="media2">
          <video src="assets/imgs/portfolio/enma/1.mp4"  loop></video>
             <span class="play-icon">
      <i class="fa-solid fa-play"></i>
    </span>
        </div>

        <div class="overlay">
          <span class="tag">Instagram Reel</span>
          <h4>Enma_Ksa</h4>
          <p> </p>
        </div>
      </div>



    <div class="reel-card video">
        <div class="media2">
          <video src="assets/imgs/portfolio/enma/2.mp4"  loop></video>
             <span class="play-icon">
      <i class="fa-solid fa-play"></i>
    </span>
        </div>

        <div class="overlay">
          <span class="tag">Instagram Reel</span>
          <h4>Enma_Ksa</h4>
          <p>   </p>
        </div>
      </div>


      <div class="reel-card video">
        <div class="media2">
          <video src="assets/imgs/portfolio/enma/3.mp4"  loop></video>
             <span class="play-icon">
      <i class="fa-solid fa-play"></i>
    </span>
        </div>

        <div class="overlay">
          <span class="tag">Instagram Reel</span>
          <h4>Enma_Ksa</h4>
          <p>   </p>
        </div>
      </div>


    </div>
  </div>
</section>

<div class="media-popup" id="mediaPopup" aria-hidden="true">
  <div class="media-popup__content" role="dialog" aria-modal="true" aria-label="Media preview">
    <button class="media-popup__close" type="button" aria-label="Close preview">
      <i class="fa-solid fa-xmark"></i>
    </button>
    <button class="media-popup__nav media-popup__prev" type="button" aria-label="Previous media">
      <i class="fa-solid fa-chevron-left"></i>
    </button>
    <div class="media-popup__stage"></div>
    <button class="media-popup__nav media-popup__next" type="button" aria-label="Next media">
      <i class="fa-solid fa-chevron-right"></i>
    </button>
    <div class="media-popup__counter"></div>
  </div>
</div>
 
<!-- ===== CTA ===== -->
<section class="cta">
  <span>WORK WITH US</span>
  <h2>We would love to hear about your project</h2>
  <a href="/contact">
    Let’s talk
    <i class="fa-solid fa-arrow-right"></i>
  </a>
</section>

 </main>

      <%- include('components/footer.ejs'); -%>

    </div>
  </div>


  <!-- All JS files -->
  <script src="assets/js/jquery-3.6.0.min.js"></script>
  <script src="assets/js/bootstrap.bundle.min.js"></script>
  <script src="assets/js/swiper-bundle.min.js"></script>
  <script src="assets/js/counter.js"></script>
  <script src="assets/js/gsap.min.js"></script>
  <script src="assets/js/ScrollTrigger.min.js"></script>
  <script src="assets/js/ScrollToPlugin.min.js"></script>
  <script src="assets/js/ScrollSmoother.min.js"></script>
  <script src="assets/js/SplitText.min.js"></script>
  <script src="assets/js/chroma.min.js"></script>
  <script src="assets/js/mixitup.min.js"></script>
  <script src="assets/js/vanilla-tilt.js"></script>
  <script src="assets/js/jquery.meanmenu.min.js"></script>
  <script src="assets/js/main.js"></script>

<script>
document.querySelectorAll(".reel-card.video video").forEach(video => {
  const card = video.closest(".reel-card");

  card.addEventListener("mouseenter", () => {
    video.play().catch(()=>{});
  });

  card.addEventListener("mouseleave", () => {
    video.pause();
    video.currentTime = 0;
  });
});
</script>

<script>
(() => {
  const popup = document.getElementById("mediaPopup");
  if (!popup) return;

  document.body.appendChild(popup);

  const stage = popup.querySelector(".media-popup__stage");
  const closeButton = popup.querySelector(".media-popup__close");
  const prevButton = popup.querySelector(".media-popup__prev");
  const nextButton = popup.querySelector(".media-popup__next");
  const counter = popup.querySelector(".media-popup__counter");
  let items = [];
  let currentIndex = 0;

  const normalizeSrc = (src) => {
    if (!src) return "";
    return src.replace(window.location.origin + "/", "");
  };

  const pauseGridVideos = () => {
    document.querySelectorAll(".reel-card.video video").forEach((video) => {
      video.pause();
      video.currentTime = 0;
    });
  };

  const collectItems = (card) => {
    const video = card.querySelector("video");
    if (video) {
      return [{
        type: "video",
        src: normalizeSrc(video.currentSrc || video.getAttribute("src"))
      }];
    }

    return Array.from(card.querySelectorAll("img"))
      .map((img) => ({
        type: "image",
        src: normalizeSrc(img.currentSrc || img.getAttribute("src")),
        alt: img.getAttribute("alt") || "Enma preview"
      }))
      .filter((item) => item.src);
  };

  const updateControls = () => {
    const hasMultiple = items.length > 1;
    prevButton.hidden = !hasMultiple;
    nextButton.hidden = !hasMultiple;
    counter.hidden = !hasMultiple;
    counter.textContent = hasMultiple ? `${currentIndex + 1} / ${items.length}` : "";
  };

  const renderItem = () => {
    stage.innerHTML = "";
    const item = items[currentIndex];
    if (!item) return;

    if (item.type === "video") {
      const video = document.createElement("video");
      video.src = item.src;
      video.controls = true;
      video.autoplay = true;
      video.muted = true;
      video.playsInline = true;
      video.preload = "auto";
      video.addEventListener("loadeddata", () => {
        video.play().catch(() => {});
      });
      stage.appendChild(video);
    } else {
      const image = document.createElement("img");
      image.src = item.src;
      image.alt = item.alt;
      stage.appendChild(image);
    }

    updateControls();
  };

  const openPopup = (nextItems) => {
    items = nextItems;
    currentIndex = 0;
    pauseGridVideos();
    renderItem();
    popup.classList.add("is-open");
    popup.setAttribute("aria-hidden", "false");
    document.body.classList.add("media-popup-open");
  };

  const closePopup = () => {
    popup.classList.remove("is-open");
    popup.setAttribute("aria-hidden", "true");
    document.body.classList.remove("media-popup-open");
    stage.querySelectorAll("video").forEach((video) => video.pause());
    stage.innerHTML = "";
  };

  const showOffset = (offset) => {
    if (!items.length) return;
    currentIndex = (currentIndex + offset + items.length) % items.length;
    renderItem();
  };

  document.querySelectorAll(".reel-card").forEach((card) => {
    card.addEventListener("click", () => {
      const nextItems = collectItems(card);
      if (nextItems.length) openPopup(nextItems);
    });
  });

  closeButton.addEventListener("click", closePopup);
  prevButton.addEventListener("click", () => showOffset(-1));
  nextButton.addEventListener("click", () => showOffset(1));
  popup.addEventListener("click", (event) => {
    if (event.target === popup) closePopup();
  });
  document.addEventListener("keydown", (event) => {
    if (!popup.classList.contains("is-open")) return;
    if (event.key === "Escape") closePopup();
    if (event.key === "ArrowLeft") showOffset(-1);
    if (event.key === "ArrowRight") showOffset(1);
  });
})();
</script>

</body>
</html>
